apiVersion: controller.azure.github.com/v1alhpa1
kind: AppDeployment
metadata:
  labels:
    app.kubernetes.io/name: operation-cache-controller
    app.kubernetes.io/managed-by: kustomize
  name: appdeployment-sample
spec:
  # TODO(user): Add fields here
